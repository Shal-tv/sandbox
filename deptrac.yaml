# deptrac.yaml
parameters:
    paths:
        - ./src
    exclude_files:
        - '#.*test.*#'
    layers:
        -   name: Controller
            collectors:
                -   type: classLike
                    regex: .*Controller.*
        -   name: Service
            collectors:
                -   type: classLike
                    regex: .*Service.*
        -   name: Repository
            collectors:
                -   type: classLike
                    regex: .*Repository.*
    ruleset:
        Controller:
            - Service
        Service:
            - Repository
