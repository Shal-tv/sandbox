name: Deploy to VPS

on:
    push:
        branches:
            - master

jobs:
    deploy:
        runs-on: ubuntu-latest

        environment:
            name: staging

        steps:
            -   name: Checkout code
                uses: actions/checkout@v4.2.1

            -   name: rsync deployments
                uses: burnett01/rsync-deployments@7.0.1
                with:
                    switches: -avzr --delete
                    path: src/
                    remote_path: ${{ secrets.DEPLOY_PATH }}
                    remote_host: ${{ secrets.SERVER_HOST }}
                    remote_port: ${{ secrets.SSH_PORT }}
                    remote_user: ${{ secrets.SERVER_USER }}
                    remote_key: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}
                    remote_key_pass: ${{ secrets.SSH_PASSPHRASE }}

#            -   name: Sync code to VPS
#                run: |
#                    sshpass -p "${{ secrets.SSH_PASSPHRASE }}" rsync -avz --delete -e "ssh -o -p ${{ secrets.SSH_PORT }} -i "${{ secrets.SERVER_SSH_PRIVATE_KEY }}"" ./ ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.DEPLOY_PATH }}
